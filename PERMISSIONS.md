# ระบบสิทธิ์และการแบ่งขอบเขตข้อมูล

## แนวคิดหลัก

สิทธิ์ในระบบนี้แยกเป็น **2 แกนอิสระ** ที่ต้องผ่านทั้งคู่:

```
สิทธิ์ที่ใช้ได้จริง  =  บทบาท (ทำอะไรได้)  ×  ขอบเขตข้อมูล (ทำกับใครได้)
                        └─ Role enum          └─ OrgNode subtree
```

ตัวอย่าง: เจ้าของร้านค้ามีสิทธิ์ `manage-members` (แกนที่ 1 ผ่าน)
แต่จะแก้ไขได้เฉพาะผู้ใช้ที่อยู่ในร้านตัวเองและผู้ขายใต้สังกัด (แกนที่ 2 จำกัดไว้)
ต่อให้ยิง request ตรงไปที่ user id ของคนในคลังใหญ่ ก็จะได้ 403

## แกนที่ 1: บทบาท (`App\Enums\Role`)

| บทบาท | จัดการสมาชิก | เปิดหน่วยงาน | อนุมัติโอน | ส่งของ | รับของ | ขาย | ปรับยอด | ดูรายงาน | จัดการสินค้า |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
| ผู้ดูแลระบบ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| ผู้จัดการคลัง | ✓ | ✓ | ✓ | ✓ | ✓ | — | ✓ | ✓ | — |
| ตัวแทนขาย | ✓ | ✓ | ✓ | ✓ | ✓ | — | — | ✓ | — |
| เจ้าของร้านค้า | ✓ | ✓ | — | ✓ | ✓ | ✓ | — | ✓ | — |
| ผู้ขาย | — | — | — | — | ✓ | ✓ | — | ✓ | — |
| ผู้ดูข้อมูล | — | — | — | — | — | — | — | ✓ | — |

## แกนที่ 2: ขอบเขตข้อมูล

ทุก query ที่แตะข้อมูลหน่วยงานต้องกรองด้วย `$user->visibleNodeIds()` ซึ่งคืน
**โหนดของตัวเอง + ลูกหลานทุกชั้น** โดยอาศัยคอลัมน์ `path`:

```php
// เร็วมาก ไม่ต้อง recursive query
OrgNode::where('path', 'like', $node->childPathPrefix() . '%');
```

ผลลัพธ์จริงจากระบบที่ทดสอบแล้ว:

| ผู้ใช้ | เห็นกี่หน่วยงาน | สต๊อกที่เห็น | สมาชิกที่จัดการได้ |
|---|---|---|---|
| เจ้าของระบบ | 6 | 893 | 5 |
| ตัวแทนขาย | 3 | 97 | 2 |
| ผู้ขาย | 1 | 0 | 0 |

## กฎความปลอดภัยที่บังคับใช้ (ทดสอบแล้วทุกข้อ)

| กฎ | ผลลัพธ์ |
|---|---|
| ผู้ขายเปิดหน้าจัดการสมาชิก | **403** |
| ผู้ขายจัดการเจ้าของร้าน (คนระดับสูงกว่า) | **false** |
| ร้านค้าสร้างสมาชิกในคลังใหญ่ (นอกสายงาน) | **403** |
| ร้านค้าตั้งใครเป็น `system_admin` | **403** |
| ผู้ใช้ที่ถูกระงับพยายามเข้าระบบ | **เข้าไม่ได้** |
| ผู้ใช้ถูกระงับระหว่างที่ยัง login ค้าง | **เตะออกทันที** (middleware `active.user`) |
| ดูหน่วยงานนอกสายงาน | **403** |
| โอนของข้ามระดับ (คลังใหญ่ → ร้านค้าโดยตรง) | **ถูกปฏิเสธ** |
| โอนเกินสต๊อกที่มี | **ถูกปฏิเสธ** |
| ล็อกอินผิด 5 ครั้ง | **ล็อก 5 นาที** |
| สแกน QR ใบเดิมซ้ำ | **ไม่ได้คะแนนเพิ่ม** (conditional update กัน race condition) |
| กรอกรหัสใต้ฟิล์มผิด | **ไม่ได้คะแนน และ QR ไม่ถูกเผา** ยังใช้ได้อยู่ |
| ลูกค้าที่ถูกระงับสแกน | **ถูกบล็อก** |
| สแกนถี่เกินกำหนด | **rate limit** ต่อลูกค้า/วัน และต่อ IP/ชั่วโมง |
| ออก QR เกินจำนวนที่ผลิตในล็อต | **ถูกปฏิเสธ** |
| เลขล็อตซ้ำในสินค้าเดียวกัน | **ถูกปฏิเสธ** |
| แลกของรางวัลเกินคะแนนที่มี | **ถูกปฏิเสธ** ไม่หักคะแนน ไม่ตัดของรางวัล |
| ยกเลิกการแลกของรางวัล | **คืนคะแนน + คืนของเข้าสต๊อก** |
| ดำเนินการรายการแลกที่จบแล้วซ้ำ | **ถูกปฏิเสธ** (กันคืนคะแนนซ้ำ) |
| ปรับคะแนนด้วยมือโดยไม่ระบุเหตุผล | **ถูกปฏิเสธ** (บังคับเพื่อตรวจสอบย้อนหลัง) |
| นับสต๊อกแล้วเว้นช่องว่าง | **ไม่แตะยอด** (ไม่ตีความว่านับได้ 0) |
| ปลายทางอนุมัติใบโอนที่ส่งมาหาตัวเอง | **403** (อนุมัติได้เฉพาะต้นทาง) |
| ปลายทางกดรับของก่อนต้นทางกดส่ง | **403** |
| อนุมัติ/ส่ง/รับ ใบโอนที่จบวงจรแล้วซ้ำอีกรอบ | **403** ทั้งสามปุ่ม |
| ผู้ขายอนุมัติใบโอน | **403** |
| เปิดบิลขายในนามหน่วยงานนอกสายงาน | **403** |
| คลังใหญ่/คลังย่อย/ตัวแทนเปิดหน้า POS | **403** (ขายให้ลูกค้าได้เฉพาะร้านค้า/ผู้ขาย) |
| ขายเกินสต๊อกที่มี | **ถูกปฏิเสธ** — ไม่สร้างบิล ไม่ตัดสต๊อก (rollback ทั้งก้อน) |
| ยกเลิกบิลขาย | คืนสต๊อกกลับครบตามจำนวนเดิม |

### ผลทดสอบสิทธิ์เข้าหน้าเว็บ (ทดสอบจริงผ่าน HTTP)

| หน้า | เจ้าของระบบ | คลังใหญ่ | ตัวแทน | ร้านค้า | ผู้ขาย |
|---|---|---|---|---|---|
| `/pos` | 200 | **403** | **403** | 200 | 200 |
| `/transfers/create` | 200 | 200 | 200 | 200 | **403** |
| `/transfers/{id}` (ใบ WH→SWH) | 200 | 200 | **403** | **403** | **403** |
| `/products*` (สินค้า/ล็อต/QR) | 200 | **403** | **403** | **403** | **403** |
| `/stock/count` | 200 | 200 | **403** | **403** | **403** |
| `/members` | 200 | 200 | 200 | 200 | **403** |
| `/customers*` (ดูอย่างเดียว) | 200 | 200 | 200 | 200 | 200 |
| `/pos/history`, `/nodes`, `/reports/*` | 200 | 200 | 200 | 200 | 200 (เฉพาะในสายงาน) |

ทดสอบครบ 22 หน้า × 6 บทบาท = 132 คู่ ไม่มี 5xx แม้แต่เคสเดียว

> `/customers` เปิดให้ทุกบทบาทที่มี `view-reports` **ดู** ได้ แต่การปรับคะแนนและระงับลูกค้า
> ต้องมี `manage-members` ส่วนการแก้ของรางวัลต้องมี `manage-products` — คุมแยกในหน้าเว็บด้วย `@can`

> ผู้ขายเปิด `/transfers/create` ไม่ได้เพราะเป็นระดับล่างสุด ไม่มีหน่วยงานลูกให้โอนต่อ

## จุดสำคัญที่ต้องระวังตอนพัฒนาต่อ

**1. อย่า `select()` โหนดโดยไม่เอา `path` มาด้วย** — จะทำให้ `subtreeIds()` คืนแค่ตัวเอง
และรายงานจะได้ยอดเป็น 0 แบบเงียบ ๆ (บั๊กนี้เคยเกิดจริงตอนพัฒนา และแก้ไปแล้ว
โดยใส่ guard ไว้ใน `subtreeIds()` ให้ดึงใหม่อัตโนมัติถ้า path หายไป)

**2. ทุก controller ใหม่ต้องกรองด้วย `visibleNodeIds()` เสมอ** — ไม่มี global scope
ครอบให้อัตโนมัติ ถ้าลืมจะรั่วข้ามสายงานทันที

**3. การรับของต้องบันทึกขาเข้าเท่ากับจำนวนที่ส่ง แล้วค่อยตัด `damage` แยก** —
ถ้ารับเข้าแค่จำนวนที่นับได้แล้วยังตัด damage อีก จะกลายเป็นหักซ้ำสองรอบ
ทำให้ยอดตัดออกต้นทางกับยอดรับเข้าปลายทางไม่ตรงกัน (บั๊กนี้เคยเกิดจริงและแก้ไปแล้ว
ใน `TransferService::receive()` — มีเทสต์ยืนยันว่า ส่ง 55 รับ 53 ต้องได้ปลายทาง +53 และ damage 2)

**4. ค่า enum ในฐานข้อมูลกับที่โค้ดส่งต้องตรงกัน** — `point_transactions.type` รับเฉพาะ
`earn_scan, earn_bonus, redeem, expire, adjust, reverse` และ `reward_redemptions.status` รับเฉพาะ
`pending, approved, shipped, completed, rejected` (ไม่มี `refund`/`cancelled`)
ถ้าส่งค่านอกรายการจะ error ตอน insert เท่านั้น ไม่มีใครเตือนตอนเขียนโค้ด

**5. `org_nodes` ใช้คอลัมน์ `status` ไม่ใช่ `is_active`** — (`is_active` เป็นของตาราง `users`)
ใช้ scope `OrgNode::active()` แทนการเขียน where เอง เคยพลาดตรงนี้แล้วดรอปดาวน์เลือกหน่วยงานว่างเปล่าแบบเงียบ ๆ

**6. การเพิ่มบทบาทใหม่** ให้แก้ที่ `Role` enum ที่เดียว
(`abilities()` + `defaultForLevel()`) แล้วเพิ่มชื่อ ability ลงใน
`AuthServiceProvider::boot()` ตารางสิทธิ์ในหน้าเว็บจะอัปเดตตามเอง
