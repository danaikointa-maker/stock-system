# ติดตั้งและรันบน Windows

> **หมายเหตุเรื่องภาษา**
> ข้อความในไฟล์ติดตั้ง (`.bat`) เป็น **ภาษาอังกฤษทั้งหมด**
> เพราะ Command Prompt รุ่นเก่าของ Windows ใช้ฟอนต์ที่แสดงภาษาไทยไม่ได้
> จะขึ้นเป็นตัวขยะหรือเครื่องหมายคำถาม
> คู่มือภาษาไทย (ไฟล์ `.md`) ยังอ่านได้ปกติใน Notepad หรือ VS Code



คู่มือฉบับย่อสำหรับรันระบบสต๊อกสินค้าบนเครื่อง Windows
ใช้ **SQLite** ไม่ต้องติดตั้ง MySQL

---

## สิ่งที่ต้องมีก่อน

| โปรแกรม | เวอร์ชัน | ดาวน์โหลด |
|---|---|---|
| PHP | 8.2 ขึ้นไป | https://windows.php.net/download/ (เลือก **Thread Safe** ไฟล์ zip) |
| Composer | ล่าสุด | https://getcomposer.org/Composer-Setup.exe |

> ถ้าไม่ได้ติดตั้ง Composer ไว้ สคริปต์จะดาวน์โหลดมาให้อัตโนมัติ
> แต่ **PHP ต้องติดตั้งเองก่อน**

### ติดตั้ง PHP บน Windows

1. ดาวน์โหลด PHP zip แบบ **Thread Safe** แล้วแตกไฟล์ไปที่ `C:\php`
2. เข้าไปในโฟลเดอร์ `C:\php` คัดลอกไฟล์ `php.ini-development` มาตั้งชื่อใหม่เป็น `php.ini`
3. เปิด `php.ini` แล้วเอาเครื่องหมาย `;` ข้างหน้าบรรทัดเหล่านี้ออก

   ```ini
   extension=pdo_sqlite
   extension=mbstring
   extension=openssl
   extension=fileinfo
   extension=curl
   ```

4. เพิ่ม `C:\php` เข้า Path ของระบบ
   - กด Start พิมพ์ `Environment Variables` แล้วเปิดขึ้นมา
   - เลือก `Path` ในช่อง System variables แล้วกด Edit
   - กด New แล้วใส่ `C:\php` กด OK
5. **เปิด Command Prompt ใหม่** แล้วพิมพ์ `php -v` ต้องขึ้นเวอร์ชัน

---

## วิธีติดตั้ง

**ดับเบิลคลิกไฟล์ `install-sqlite.bat`**

หรือถ้าใช้ PowerShell ให้คลิกขวาที่ `install-sqlite.bat` แล้วเลือก **Run with PowerShell**

สคริปต์จะทำให้อัตโนมัติทั้งหมด:

1. ตรวจสอบ PHP และ extension ที่จำเป็น
2. ตรวจสอบ / ดาวน์โหลด Composer
3. สร้างโปรเจกต์ Laravel ที่ `C:\Users\<ชื่อคุณ>\stock-app`
4. คัดลอกโค้ดระบบสต๊อกเข้าไป
5. ตั้งค่าฐานข้อมูล SQLite
6. สร้างตารางและใส่ข้อมูลตัวอย่าง
7. เปิดเบราว์เซอร์ที่ `http://localhost:8000`

ครั้งแรกใช้เวลาประมาณ **3-5 นาที** (ต้องดาวน์โหลด Laravel)

---

## เปิดใช้งานครั้งถัดไป

หลังติดตั้งเสร็จแล้ว ครั้งต่อไปไม่ต้องติดตั้งใหม่
**ดับเบิลคลิก `start-server.bat`** ได้เลย เปิดใน 2 วินาที

---

## บัญชีสำหรับเข้าใช้งาน

รหัสผ่านทุกบัญชีคือ **`password`**

| อีเมล | บทบาท | เห็นอะไร |
|---|---|---|
| `admin@demo.test` | เจ้าของระบบ | ทุกอย่าง |
| `wh@demo.test` | คลังใหญ่ | รับของเข้า อนุมัติใบโอน |
| `swh@demo.test` | คลังย่อย | โอนต่อให้ตัวแทน |
| `agent@demo.test` | ตัวแทนขาย | กระจายให้ร้านค้า |
| `shop@demo.test` | ร้านค้า | เปิดบิลขาย POS |
| `seller@demo.test` | ผู้ขาย | ขายหน้าร้าน |

**หน้าลูกค้าสแกน QR รับคะแนน** (ไม่ต้องล็อกอิน)
`http://localhost:8000/scan`

---

## ปัญหาที่พบบ่อย

### ขึ้นว่า `php` ไม่ใช่คำสั่งที่รู้จัก
ยังไม่ได้เพิ่ม `C:\php` เข้า Path หรือยังไม่ได้เปิด Command Prompt ใหม่
ทำตามขั้นตอน "ติดตั้ง PHP บน Windows" ข้อ 4-5 อีกครั้ง

### ขึ้นว่าขาด extension
เปิด `C:\php\php.ini` แล้วลบ `;` หน้าบรรทัด `extension=` ของตัวที่ขาด
ถ้าไม่มีไฟล์ `php.ini` ให้คัดลอก `php.ini-development` มาตั้งชื่อเป็น `php.ini` ก่อน


### พอร์ต 8000 ถูกใช้อยู่แล้ว
เปิดไฟล์ `.bat` ด้วย Notepad หาบรรทัดท้ายสุดที่มี `--port=8000` แล้วเปลี่ยนเป็นเลขอื่น เช่น `--port=8080`
แล้วเข้าเว็บที่ `http://localhost:8080` แทน

### อยากล้างข้อมูลเริ่มใหม่
เปิด Command Prompt ไปที่โฟลเดอร์ `C:\Users\<ชื่อคุณ>\stock-app` แล้วพิมพ์

```
php artisan migrate:fresh --seed
```

### ตัวหนังสือภาษาไทยเป็นตัวประหลาดใน Command Prompt
ไม่กระทบการทำงาน ตัวเว็บแสดงผลปกติ
ถ้าอยากแก้ให้ใช้ Windows Terminal แทน Command Prompt รุ่นเก่า

---

## ติดตั้งด้วย MySQL แทน SQLite

ถ้าต้องการใช้ MySQL จริง (แนะนำสำหรับใช้งานจริง) ดูขั้นตอนใน `INSTALL.md`
หรือใช้ไฟล์ `database/schema.sql` สร้างตารางตรง ๆ ได้เลย
